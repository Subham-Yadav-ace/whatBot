# MongoDB is on Atlas — only Redis is needed locally for BullMQ

services:
  redis:
    image: redis:7-alpine
    container_name: placement-bot-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data:
